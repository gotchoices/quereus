# Pre-existing test failure: quereus-store MV structural-alter staleness tests

## Command run

From repo root `C:\projects\quereus`:

```
yarn test
```

The `packages/quereus` workspace is fully green (6183 passing, 9 pending, 0 failing).
The failures are in the **`@quereus/store` (`packages/quereus-store`)** workspace, which runs
later in the same `yarn test` sweep.

## Failing tests (6, all same root cause)

`packages/quereus-store/test/mv-rehydrate-adopt.spec.ts`:
- `stale-at-close exclusion > a stale-at-close MV refills even under a clean shutdown (add column + post-stale DML)` (line 478)
- `stale-at-close exclusion > a stale-then-refreshed MV adopts (refresh clears the flag before close)` (line 506)
- `stale-at-close exclusion > fine-grained: only the stale-at-close MV refills, the live one adopts` (line 535)
- `stale-at-close exclusion > MV-over-MV: a stale upstream cascades, both refill` (line 560)

`packages/quereus-store/test/mv-store-backing.spec.ts` (both fail in a `before each` hook):
- `constraint-bearing refresh re-validation > CHECK violator on the store backing > a CHECK-violating drift throws the attribution and leaves the committed store contents intact + stale` (hook at line 449)
- `constraint-bearing refresh re-validation > child-side FK orphan on the store backing (FK enforcement on) > an FK-orphan drift throws the FK attribution and leaves the committed store contents intact + stale` (hook at line 488)

## Error excerpt

```
1) materialized-view adopt-without-refill at rehydrate
     stale-at-close exclusion
       a stale-at-close MV refills even under a clean shutdown (add column + post-stale DML):

    add column marked the MV stale
    + expected - actual

    -false
    +true
    at Context.<anonymous> (packages\quereus-store\test\mv-rehydrate-adopt.spec.ts:478:9)
```

All six fail on the same assertion shape: after `alter table src add column w integer null`, the
test asserts `derivation.stale === true`, but the actual value is now `false`.

## Why this is pre-existing (not caused by this ticket)

- **My diff is entirely in `packages/quereus`** (the `with defaults` clause move + column-rename
  propagation). I made **zero** changes in `packages/quereus-store`
  (`git status --short packages/quereus-store/` is empty).
- The failures are about **`ADD COLUMN` MV-staleness semantics**, a subsystem this ticket never
  touches. The MVs in the failing tests (`select id, v from src`) carry no `with defaults` clause.
- The behavior change is from the **already-committed** ticket `mv-restore-unaffected-structural-alters`
  (commits `d39c7a9e` implement / `177c285d` review — the two most recent on this branch). That
  commit reworked `packages/quereus/src/runtime/emit/materialized-view-helpers.ts` so a structural
  alter (e.g. unprojected `ADD COLUMN`) **restores the MV in place rather than leaving it stale**, and
  it updated the **`packages/quereus` (memory) tests** accordingly (`mv-structural-alter-restore.spec.ts`
  new, `53-materialized-views-rowtime.sqllogic`, `maintained-table-refresh-revalidation.spec.ts`,
  `covering-structure.spec.ts`, etc.). It updated **no `packages/quereus-store` test**, so those two
  store specs still assert the old `stale === true` behavior.
- These store tests had never run to completion before: every prior full `yarn test` bailed at the
  `packages/quereus` workspace on the `with-defaults-clause-rehome` schema-differ failure (now fixed),
  so Yarn never reached the `@quereus/store` workspace. Fixing the quereus failure simply unmasked
  this stale store-test discrepancy.
- The equivalent memory-backed staleness tests in `packages/quereus` all pass (6183/0) — if the new
  restore behavior were wrong, those would fail too. They don't.

## Work-around applied

None. My ticket's package (`packages/quereus`) is green (build + lint + 6183 tests). I did not disable
or modify the failing store tests. The expected resolution is to update the two
`packages/quereus-store` MV staleness specs to the post-`mv-restore-unaffected-structural-alters`
behavior (structural alter restores rather than stales), i.e. carry that ticket's store-parity over —
or confirm a genuine memory/store divergence in the restore pass and fix it in
`materialized-view-helpers.ts` for the store backing.
