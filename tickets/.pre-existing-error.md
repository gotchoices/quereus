# Pre-existing test failure

## Command

From `packages/quereus` (monorepo package `@quereus/quereus`):

```
node test-runner.mjs --store            # i.e. `yarn test:store`, QUEREUS_TEST_STORE=1
```

(Memory mode `node test-runner.mjs` / `yarn test` is fully green: 5190 passing, 0 failing.)

## Failing test

- File: `packages/quereus/test/logic/50-metadata-tags.sqllogic`
- Test: `SQL Logic Tests (Store Mode) › File: 50-metadata-tags.sqllogic › should execute statements and match results or expected errors`

Error excerpt:

```
QuereusError: Index 'uq_expo_vin' not found in schema 'main'
  at SchemaManager.updateIndexTags (packages/quereus/src/schema/manager.ts:940:19)
  at SchemaManager.mergeIndexTags (packages/quereus/src/schema/manager.ts:976:14)
  at Object.run (packages/quereus/src/runtime/emit/set-object-tags.ts:43:24)
```

Full store suite (`--no-bail`): **5184 passing, 14 pending, 1 failing** — this is the only failure.

## Why this is pre-existing (not from this ticket)

- **Verified against the clean baseline.** I `git stash`ed my two source-file edits
  (`packages/quereus-store/src/common/store-module.ts`, `packages/quereus/src/index.ts`),
  rebuilt both packages, and ran `node test-runner.mjs --store --grep "50-metadata-tags"`:
  it still fails with the identical `Index 'uq_expo_vin' not found in schema 'main'`
  (0 passing, 1 failing). The failure exists at HEAD without my changes.
- **Outside this ticket's diff.** The failure is in the engine `ALTER INDEX … SET TAGS`
  path (`SchemaManager.updateIndexTags` ← `set-object-tags.ts`) acting on an *exposed
  implicit covering index* (`uq_expo_vin`, via `quereus.expose_implicit_index`) that the
  store-mode schema manager has not registered under that name. This ticket only touched
  the store's existing-row UNIQUE re-validation (`validateUniqueOverExistingRows`,
  `buildIndexEntries`) and the `ALTER COLUMN … SET COLLATE` arm — none of which run in the
  index-tag path. It reproduces in store mode only (a store index-registration/persistence
  gap), not memory mode.

## Work-arounds applied

None. I did not disable or skip the failing test. My own ticket's tests
(`41.7.1` PK-only memory, `41.7.2` cross-module store) all pass.
