# Pre-existing test failure (not caused by `unique-constraint-honors-column-collation`)

## Command

From repo root:

```
yarn workspace @quereus/quereus run test:store        # i.e. node test-runner.mjs --store
```

(LevelDB store-module run of the `packages/quereus` logic suite.)

## Failing test

```
SQL Logic Tests (Store Mode)
  File: 51-lens-foundation.sqllogic
    should execute statements and match results or expected errors:
    Error: [51-lens-foundation.sqllogic:135] Failed executing setup SQL: apply schema appcar;
           - Error: lens: logical table 'appcar.Car' has no basis backing
```

Everything else passes: `4083 passing, 13 pending, 1 failing` (the one failure is the above).
The full memory run (`yarn test`) is fully green (`4088 passing, 9 pending`); this failure is
store-mode-only.

## Why it is pre-existing

- The error is in the **lens / logical-schema** subsystem (`apply schema ... has no basis
  backing`), which this ticket never touches. This ticket only threads a column's declared
  collation into UNIQUE-conflict comparisons (`compareSqlValues(..., collation)`) and into the
  auto-built UNIQUE index spec — in `manager.ts`, `store-table.ts`, and `isolated-table.ts`.
  None of those touch lens basis backing.
- Verified directly: `git stash` of all this ticket's source+test changes, then
  `yarn workspace @quereus/quereus run test:store --grep "51-lens-foundation"` reproduces the
  **identical** error. So it fails on the branch state prior to this ticket's edits.
- This is the `view-updates-lens` feature branch (lens work in progress, 103 commits ahead of
  origin); a store-mode lens basis-backing gap here is consistent with that WIP.

## Work-around

None needed for this ticket — the failing file is outside the diff and was left untouched. No
test was disabled or skipped to complete this ticket.
