# Pre-existing / unrelated failure

## Command
`yarn lint` (from repo root) — i.e. `yarn workspace @quereus/quereus run lint`.

## Failure
```
C:\projects\quereus\packages\quereus\src\core\database-watchers.ts
  21:7  error  'DeltaApplyInput' is defined but never used. Allowed unused vars must match /^_/u  @typescript-eslint/no-unused-vars

✖ 1 problem (1 error, 0 warnings)
```

## Why it is not mine
My ticket (`lens-parent-side-fk-nullable-key-update-gap`) only touched
`packages/quereus/src/planner/mutation/lens-enforcement.ts`,
`packages/quereus/test/lens-enforcement.spec.ts`, and `docs/lens.md`. Running
eslint on just those three files exits 0 (clean).

The lint error is in `packages/quereus/src/core/database-watchers.ts`, which —
together with `packages/quereus/src/core/database.ts` — showed up as a modified
file in the working tree at the *start* of my session even though the initial
git snapshot reported the tree clean. The diff is an unrelated feature
(`notifyExternalChange` / `notifyExternalTableChange` external-change watcher
notification for replicated-store vtabs) that I did not author. It appears to be
a half-finished concurrent change: the unused-import `DeltaApplyInput` is added
to the import list but the only use (`const input: DeltaApplyInput`) is present
in the same diff, so the eslint complaint suggests the file is mid-edit / stale.

I left those two files untouched (did not stage, revert, or fix them) since they
are outside my ticket's scope.
