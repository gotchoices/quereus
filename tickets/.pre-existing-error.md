## Pre-existing failure: docs size ratchet (schema.md, sql.md)

Command: `node scripts/check-docs.mjs` (also `yarn docs:check`, run from repo root)

Failing output (reproduces at HEAD `de067a70`, before any changes from
`fix/docs-invariants-conventions`):

```
docs/schema.md: 16310 words exceeds its ratchet of 15683 (+627) — a doc may shrink, never grow
docs/sql.md: 28853 words exceeds its ratchet of 28654 (+199) — a doc may shrink, never grow
```

Why pre-existing: verified with `git stash` — the same two failures reproduce on a clean
checkout of `main` at `de067a70`, with no working-tree changes at all. The
`docs-invariants-conventions` ticket's diff touches only `docs/invariants.md` and
`packages/quereus/test/optimizer/side-effect-audit.spec.ts` (splitting two over-long/
malformed invariant entries); it never touches `docs/schema.md` or `docs/sql.md`, so it
cannot be the cause of either doc growing past its recorded ratchet.
