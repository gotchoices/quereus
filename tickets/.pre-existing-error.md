# Pre-existing failure: lint error in an untracked temporal-literal probe

## Command

From repo root:

```
yarn workspace @quereus/quereus run lint
```

(eslint over `src/**/*.ts` `test/**/*.ts` in `packages/quereus`)

## Failure

```
C:\projects\quereus\packages\quereus\test\zzz-temporal-literal-probe.spec.ts
  2:10  error  'expect' is defined but never used. Allowed unused vars must match /^_/u  @typescript-eslint/no-unused-vars

✖ 1 problem (1 error, 0 warnings)
```

## Why this is pre-existing (not mine)

- `packages/quereus/test/zzz-temporal-literal-probe.spec.ts` is an **untracked** file
  (`git status` shows it as `??`) that I did not create. My ticket
  (`reserved-tag-namespace-typed-registry`) added only `src/schema/reserved-tags.ts`,
  `test/schema/reserved-tags.spec.ts`, `test/logic/53-reserved-tags.sqllogic`, and edits to
  `src/schema/lens-compiler.ts` + two docs — none of which `git diff` / lint flag.
- The file is a leftover diagnostic probe for **temporal comparison-literal canonicalization**
  (it only `console.log`s, asserts nothing) — a subsystem this ticket never touches. It imports
  `expect` from chai but never uses it, which is the lint error.
- `yarn test` passes with this file present (its three `it`s have no assertions), so only the
  lint gate is affected.

## Work-around taken

None. I left the stray file untouched (I didn't create it, so I surfaced it rather than
deleting or editing it). My own new/modified files lint clean — eslint reported exactly this one
problem, in this one unrelated file. Suggested resolution: delete the stray probe file, or add
the unused-import fix; it appears to be debugging residue that was never meant to be committed.
