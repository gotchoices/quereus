# Pre-existing store-mode failure: 50.2-declare-schema-renames.sqllogic

## Command

From `packages/quereus`:

```
yarn test:store
# narrowed repro:
node test-runner.mjs --store --grep "50.2-declare-schema-renames"
```

(`yarn test` — memory mode — is fully green: 5683 passing.)

## Failing test

`SQL Logic Tests (Store Mode) › File: 50.2-declare-schema-renames.sqllogic › should execute statements and match results or expected errors`

```
Error: [50.2-declare-schema-renames.sqllogic:802] Failed executing setup SQL: apply schema main; - Error: Failed to execute DDL: DROP INDEX IF EXISTS tri_old
Error: dropIndex failed for index 'tri_old' on table 'src_new': Store table 'src_new' not found in schema 'main'
```

## Why pre-existing

Reproduced byte-identically at HEAD with all of this ticket's changes stashed
(`git stash` → same single failure, same message → `git stash pop`). The error
originates inside the store plugin's own `dropIndex` (its internal table
registry doesn't know `src_new` after the lens-driven rename), which is reached
*before* any of the schema-change notify changes this ticket made fire; the
ticket's diff touches no store-module code and no rename-propagation code.

## Work-arounds taken

None — the failure was left as-is; the ticket's own validation relied on
`yarn test` (green) plus a targeted `--store` run confirming the failure is
unrelated and unchanged by the diff.
