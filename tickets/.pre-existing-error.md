# Pre-existing test failures: maintained-table DDL/rehydrate (quereus core + quereus-store)

## Commands run

- `yarn test` (repo root, full workspace suite) — 5665 passing, 1 failing:
  - `view persistence: generateMaintainedTableDDL fixed point — always emits a fully-qualified (schema.name) MV name; explicit-default USING normalizes away` (`packages/quereus/test/view-mv-ddl-persistence.spec.ts:627`)
  - `AssertionError: expected 'CREATE TABLE "main"."v" (PRIMARY KEY ()) maintained as select 1 as a' to match /^create materialized view main\.v /i`
- `node --import ./packages/quereus/register.mjs node_modules/mocha/bin/mocha.js "packages/quereus/test/view-mv-ddl-persistence.spec.ts" --reporter min` (repo root, spec in isolation) — 44 passing, **14 failing**, all of the same shape: `generateMaintainedTableDDL` emits `CREATE TABLE ... maintained as ...` where the tests expect `create materialized view ...` (e.g. "generated DDL re-parses to createMaterializedView: expected createMaterializedView, actual createTable"). The isolation-vs-full-suite delta (14 vs 1) indicates order-dependence — some earlier spec in the full run leaves state that masks most of these.
- `yarn workspace @quereus/store run test` — 533 passing, **2 failing**, both in `packages/quereus-store/test/mv-rehydrate-adopt.spec.ts` ("materialized-view adopt-without-refill at rehydrate"): "a source shape change between sessions fails the shape gate: refill matches the new shape" and "MV-over-MV: a refilled upstream forces the dependent to refill (adopt-ledger gate)" — both `AssertionError: expected [...] to have a length of +0 but got N` at `mv-rehydrate-adopt.spec.ts:215` and the MV-over-MV block. Thematically the same maintained-table DDL/rehydrate subsystem as the core failures.

## Why pre-existing

My ticket (`sync-adapter-ingest-via-seam`) touches only `packages/quereus-sync` (src + tests), `packages/quoomb-web/src/worker/quereus.worker.ts`, and docs/READMEs. Nothing under `packages/quereus` or `packages/quereus-store` changed (`git status` confirms), and the failing specs exercise core MV DDL generation (`generateMaintainedTableDDL`) and store MV rehydrate/adopt — neither has any dependency on the sync package. The `CREATE TABLE ... maintained as` output shape suggests a regression or intentional-but-untested change from recent maintained-table/lens work on this branch (`view-updates-lens`, e.g. the `maintained-table-reshape-narrowing-attr-on-stale-data` ticket), predating this ticket.

## Workarounds taken

None — the failure does not intersect this ticket's code paths; no test was disabled or modified.
