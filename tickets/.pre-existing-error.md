# Pre-existing (flaky) test failure — NOT caused by ticket 1-planner-cost-model-double-count

## Failing test

- Package: `@quereus/sync-coordinator` (`packages/sync-coordinator`)
- Test: `StoreManager > disk eviction > should clear eviction candidates on shutdown`
- Path: `packages/sync-coordinator/test/store-manager.spec.ts:319`
- Error excerpt:
  ```
  AssertionError: expected +0 to equal 1
  + expected - actual
  -0
  +1
  at Context.<anonymous> (packages/sync-coordinator/test/store-manager.spec.ts:319:54)
  ```

## Commands run

- Full suite (from repo root): `yarn test` — this run surfaced the failure (1 failing in `@quereus/sync-coordinator`, everything else green; the core `@quereus/quereus` suite passed 6537).
- Isolated re-run: `yarn workspace @quereus/sync-coordinator run test` — **passed 128/128** (the same test passed).

## Why this is pre-existing / not mine

- It is **flaky/intermittent**: it failed once inside the full-suite run but passed on an isolated re-run of the same package, so it is a timing/ordering-sensitive eviction test, not a deterministic break.
- It lives in `@quereus/sync-coordinator`, a package this ticket never touches. This ticket's diff is confined to `packages/quereus/src/planner/nodes/*`, `packages/quereus/src/planner/validation/plan-validator.ts`, `packages/quereus/test/planner/cost-additivity.spec.ts`, and `docs/optimizer.md` — none of which is on any code path the sync-coordinator store-manager exercises.
