# Pre-existing test failure

## Command
From `packages/quereus`:

```
node test-runner.mjs        # i.e. `yarn test` for @quereus/quereus
```

(The runner ignores a single-spec path argument and always runs the full suite.)

## Failing test
`packages/quereus/test/mv-rename-propagation.spec.ts:70`

> MV rename propagation: derived fields and events
>   "TABLE rename re-keys sourceTables/bodyHash/sql and fires materialized_view_modified"

Excerpt:

```
regenerated DDL re-parses
+ expected - actual
-createTable
+createMaterializedView
```

The MV's regenerated DDL round-trips back as a `createTable` statement instead of
`createMaterializedView` after a source-table rename.

## Why it is pre-existing
- This `fix`-stage ticket produced **only** a markdown ticket file under
  `tickets/implement/` — it made **no source changes** at all, so this failure
  exists at HEAD independently of any work here.
- The failure is in the **MV DDL-stringify / rename-propagation** subsystem
  (`ast-stringify` / `viewDefinitionToCanonicalString` round-trip), which this
  ticket (reshape attribute-shift deferral in `materialized-view-helpers.ts`)
  never touches.
- Reproduced standalone (1 failing / 2158 passing) with a clean working tree.

## Work-around
None applied — the failure is unrelated to the reshape work and was left as-is.
