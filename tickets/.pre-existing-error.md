# Pre-existing test failure

**Command** (from `packages/quereus`): `yarn test`
(reproduce singly: `yarn test:single "packages/quereus/test/logic.spec.ts" --grep "07.7-scalar-agg"`)

**Failing test:** SQL Logic Tests › File: 07.7-scalar-agg-decorrelation.sqllogic › should execute statements and match results or expected errors
Path: `packages/quereus/test/logic/07.7-scalar-agg-decorrelation.sqllogic` (the "ORDER BY on an aggregate (GROUP BY) query" case near the end of the file)

**Error excerpt:**

```
Scalar subquery returned more than one row (at line 1, column 62)
SELECT p.k AS gk, count(*) AS c FROM p GROUP BY p.k ORDER BY (SELECT count(*) FROM d WHERE d.k = p.k), p.k
                                                             ^
  QuereusError: Scalar subquery returned more than one row
      at Object.run (src/runtime/emit/subquery.ts:63:23)
```

**Why pre-existing:** Fails identically with the change from ticket
`3-feat-decorrelate-exists-in-select-list` disabled (verified by unregistering
the new `exists-in-select-decorrelation` rule and re-running — same failure).
The failing query contains no EXISTS/IN; it is a correlated scalar-aggregate
subquery in an ORDER BY key over a GROUP BY query — exactly the scope of the
still-in-flight ticket `feat-decorrelate-scalar-subquery-order-by`
(`tickets/implement/2-feat-decorrelate-scalar-subquery-order-by.md`, carrying a
resume note from a timed-out run). That ticket's partially-landed work added
both the `scalar-agg-decorrelation-sort` rule and this test; the GROUP-BY-outer
case appears to be the unfinished part. Expect its resumed run to fix this;
no separate fix ticket should be needed unless that ticket lands without it.
