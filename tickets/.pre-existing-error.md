# Pre-existing test failure

## Command
From `packages/quereus`:

```
node test-runner.mjs --grep "41.7.1" --reporter spec
```

(equivalently surfaces in any full `yarn workspace @quereus/quereus test` run, memory mode)

## Failing test
- File: `packages/quereus/test/logic/41.7.1-alter-column-collate-unique.sqllogic` (line 46)
- Mocha test: `SQL Logic Tests › File: 41.7.1-alter-column-collate-unique.sqllogic › should execute statements and match results or expected errors`
- Error excerpt:
  ```
  Error: [41.7.1-alter-column-collate-unique.sqllogic:46] Expected error matching
  "UNIQUE constraint failed" but SQL block executed successfully.
  ```

## Why this is pre-existing / unrelated
- Verified on clean HEAD (`git stash` → run → `0 passing, 1 failing` with the identical error → `git stash pop`).
- It is in the `ALTER COLUMN ... SET COLLATE` path (memory module re-keying/re-validating PK+UNIQUE under a collation change). This ticket only touches `ALTER TABLE ADD COLUMN` CHECK enforcement (`alter-table.ts` build/emit + node) — it never touches SET COLLATE, the memory re-key path, or UNIQUE enforcement.
- The recent commit `7e7e290b tess: agent error on alter-column-set-collate — added resume note` indicates this SET COLLATE area is already a known in-progress/errored ticket. The file is also already listed in the store-mode skip set in `test/logic.spec.ts`.

## Work-around
- None applied. The failure was not disabled or modified. My ticket's tests (`03.4-defaults.sqllogic` ADD COLUMN CHECK cases, memory + store; `90.2.1-alter-extra-errors.sqllogic §3`) pass independently.
