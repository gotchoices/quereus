# Pre-existing test failure

## Command
From repo root: `yarn test` (default across-workspaces run). The failing spec lives
in the `@quereus/quereus-store` workspace, not `@quereus/quereus`.

Observed while running `node test-runner.mjs` (packages/quereus) with a broad
`--grep` that incidentally matched the store spec via the shared mocha pattern.

## Failing test
File: `packages/quereus-store/test/seed-reopen-repro.spec.ts`
Test: `REPRO: declarative seed reopen › reopen + apply schema with seed WITHOUT rehydrate`

Error excerpt:
```
QuereusError: Failed to apply seed data for table tablemetadata.
SQL: INSERT INTO tablemetadata VALUES (1, 'AllSite'); INSERT INTO tablemetadata VALUES (2, 'Other')
Error: UNIQUE constraint failed: tablemetadata PK.
  Caused by: ConstraintError: UNIQUE constraint failed: tablemetadata PK.
```
(The companion test "…WITH rehydrate" passes.)

## Why pre-existing
- It is a leftover `REPRO:` spec from the just-completed, unrelated ticket
  `declarative-seed-reopen-pk-collision` (see recent commits a4ca4d7d / c4f7a6a6 /
  7e8ac0ce). It is in the `quereus-store` package, entirely outside this ticket's
  diff (this ticket only touches `packages/quereus/src/runtime/emit/table-valued-function.ts`
  and a quereus test).
- It fails at HEAD before any of my edits — my ticket never touches the
  declarative-seed / store seeding path.

## Work-around
None needed for this ticket — my own work is in `packages/quereus`, and the
TVF/window/json test groups (101 tests) pass. I did not disable or modify the
store spec (not my file to touch).
