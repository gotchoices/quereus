## Pre-existing test failure

**Command:** `yarn test` (run from `packages/quereus`)

**Failing test:** `view persistence: importCatalog materialized-view re-materialization rehydrates the with defaults clause — MV write-through supplies the defaulted source column`
Path: `packages/quereus/test/view-mv-ddl-persistence.spec.ts`

**Error excerpt:**
```
Error: Timeout of 10000ms exceeded. For async tests and hooks, ensure "done()" is called; if returning a Promise, ensure it resolves. (C:\projects\quereus\packages\quereus\test\view-mv-ddl-persistence.spec.ts)
```

**Why pre-existing:** ticket `sqllogic-split-47.3-index-derived-collation` only touches
`packages/quereus/test/logic/47.3-upsert-conflict-target-collation.sqllogic` and adds a new
`packages/quereus/test/logic/47.3.1-upsert-conflict-index-derived-collation.sqllogic` — no `.ts` source or
materialized-view code was touched. Full suite otherwise green (6587 passing, 13 pending, 1 failing).
