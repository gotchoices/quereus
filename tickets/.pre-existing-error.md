# Pre-existing test failure (flagged by 1-bx-roundtrip-law-harness)

## Command

Full monorepo run from repo root:

```
yarn test
```

The failure is in the `@quereus/quereus` workspace
(`packages/quereus/test/property-planner.spec.ts`).

## Failure

```
1 failing
Error: Timeout of 2000ms exceeded. For async tests and hooks, ensure "done()" is
called; if returning a Promise, ensure it resolves.
(C:\projects\quereus\packages\quereus\test\property-planner.spec.ts)
```

## Why this is pre-existing / unrelated to this ticket

- This ticket only adds a new `describe('View Round-Trip Laws')` block to a
  **different** file — `packages/quereus/test/property.spec.ts`. It touches no
  engine code and does not modify `property-planner.spec.ts`.
- The failure is a **2000ms per-test timeout**, not an assertion failure — a
  resource/scheduling symptom, not a logic regression.
- `property-planner.spec.ts` is **green in isolation**:
  `node --import ./packages/quereus/register.mjs node_modules/mocha/bin/mocha.js
  "packages/quereus/test/property-planner.spec.ts"` → `27 passing (46s)`. The 46s
  wall-clock for 27 fast-check-heavy tests shows individual cases run close to the
  2000ms budget; under the parallel full-monorepo `yarn test` (multiple workspace
  mocha processes contending for CPU) one slips past the timeout.
- The sequential single-package run also passes the file:
  `yarn workspace @quereus/quereus run test` → `3924 passing, 9 pending, 0 failing`.

## Workaround applied

None needed for this ticket — the new View Round-Trip Laws block and the rest of
the quereus suite pass both standalone and in the sequential package run. No
change was made to disable or skip `property-planner.spec.ts`.

Possible durable fix for the triage pass: raise the mocha timeout for
`property-planner.spec.ts` (e.g. `this.timeout(…)` or a per-suite override) so the
heavy fast-check cases are not bound by the 2000ms default under parallel load.
