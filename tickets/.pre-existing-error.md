# Pre-existing / concurrent-edit lint failure

## Command
`yarn workspace @quereus/quereus run lint`
(run from repo root `C:\projects\quereus`; the failure is in the `tsc -p tsconfig.test.json --noEmit` typecheck phase of the lint script, not eslint)

## Failure
```
src/vtab/memory/layer/safe-iterate.ts(17,21): error TS2341: Property 'keyFromEntry' is private and only accessible within class 'BTree<TKey, TEntry>'.
src/vtab/memory/layer/safe-iterate.ts(22,21): error TS2345: Argument of type 'TKey | undefined' is not assignable to parameter of type 'TKey'.
  'TKey' could be instantiated with an arbitrary type which could be unrelated to 'TKey | undefined'.
```

Failing file: `packages/quereus/src/vtab/memory/layer/safe-iterate.ts` (calls `tree.keyFromEntry(...)` and `tree.find(currentKey)` where `currentKey: TKey | undefined`).

## Why this is not from ticket `runtime-tee-merge-iterator-leak`
- This ticket touches only `packages/quereus/src/runtime/async-util.ts` and `packages/quereus/test/runtime/async-util.spec.ts` (runtime stream helpers). It never touches the `vtab/memory` BTree layer.
- The failure is caused by a **concurrent, currently-uncommitted** dependency bump present in the working tree (another in-flight ticket, likely the one editing `inheritree-cow-delete.spec.ts`): `packages/quereus/package.json` + `yarn.lock` bump `inheritree` `^0.4.0` → `^1.0.1`. inheritree v1 made `BTree.keyFromEntry` **private**, which breaks `safe-iterate.ts`. Committed HEAD still pins `inheritree ^0.4.0`, under which lint is clean.
- Verified clean-in-isolation: lint returned exit 0 twice on this ticket's diff *before* the concurrent inheritree bump landed in the working tree. Full test suite still passes (6510 passing, 0 failing) because TypeScript `private` is compile-time only.

## Suggested disposition for triage
Ownership belongs to the concurrent `inheritree` 0.4.0→1.0.1 upgrade work (the ticket editing `vtab/memory/index.ts`, `transaction.ts`, `inheritree-cow-delete.spec.ts`). `safe-iterate.ts` must be updated for the v1 API: `keyFromEntry` is no longer public (use the supported v1 accessor / path API), and `tree.find` now needs a defined `TKey` (guard the `currentKey: TKey | undefined`). If that ticket has already committed its own `safe-iterate.ts` fix by the time triage runs, this will already be green — no action needed.
