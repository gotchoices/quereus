# Pre-existing / external test failure

## Command
From repo root:

```
yarn workspace @quereus/quereus test
```

(The full `packages/quereus` Mocha suite; default memory-backed vtab.)

## Failing test
```
SQL Logic Tests
  File: 44.1-nondeterministic-schema.sqllogic
    should execute statements and match results or expected errors
```

Error excerpt (`44.1-nondeterministic-schema.sqllogic:262`):
```
Block: CREATE TABLE relaxed_default_colref (
    id INTEGER PRIMARY KEY,
    a INTEGER,
    b INTEGER DEFAULT (a)
);
Expected error containing: "may not reference columns"
Actual error: "DEFAULT for column 'b' in table 'relaxed_default_colref' may not reference a
bare column; use 'new.<column>' to read a value supplied by the INSERT, or a generated column
instead."
```

## Why this is not from my ticket (`view-write-nway-inner-join`, review stage)
- My review touched **only** `packages/quereus/src/planner/mutation/multi-source.ts`,
  `docs/view-updateability.md`, and `packages/quereus/test/logic/93.4-view-mutation.sqllogic`
  (the multi-source view-mutation join path — a `.reverse()` of the n-way DELETE fan-out order
  plus a regression test and doc updates).
- The failure is in the **DEFAULT-column-reference** subsystem. The working tree contains a
  separate, uncommitted changeset I did **not** author:
  `src/planner/building/insert.ts`, `src/schema/manager.ts`, `test/logic/03.4-defaults.sqllogic`,
  `test/logic/44.1-nondeterministic-schema.sqllogic`, `test/logic/46-mutation-context.sqllogic`
  (116 insertions), plus an untracked `packages/quereus/fulltest_run.log`. That changeset
  reworks the DEFAULT bare-column-reference error message to the new "may not reference a bare
  column …" wording but did not update the expectation at `44.1-…:262`, which still expects the
  old substring "may not reference columns".
- This is almost certainly a concurrent agent's in-flight work in the same shared working
  directory (the stray `fulltest_run.log` corroborates a separate full-test run).

## Proof it is independent of my changes
- `git stash` of the **entire** working tree (all uncommitted changes, mine and the external
  ones) → `yarn workspace @quereus/quereus test` passes **4626 passing, 9 pending, 0 failing**.
- The 44.1 test fails deterministically in isolation only with the external `insert.ts` /
  `manager.ts` changes present; it is unrelated to view mutations.
- My own targeted run `--grep "93.4"` passes (1 passing), and `typecheck` + `lint` are clean.

## Work-arounds applied
None. I did not modify or disable the failing test, and I did not touch the external
DEFAULT-subsystem files. My ticket's changes are isolated to the multi-source view-mutation path.
