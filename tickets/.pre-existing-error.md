# Pre-existing test failure

## Command
From repo root: `yarn workspace @quereus/quereus test`
(equivalently, in `packages/quereus`: `node test-runner.mjs`)

Isolated repro: `node test-runner.mjs --reporter spec --grep "propagated INDs never over-claim"`

## Failing test
`IND soundness (no over-claim) > propagated INDs never over-claim on materialized optimized plans`
File: `packages/quereus/test/optimizer/inclusion-dependencies.spec.ts`

Error excerpt:
```
Error: Timeout of 2000ms exceeded. For async tests and hooks, ensure "done()" is called;
if returning a Promise, ensure it resolves.
```
Run in isolation the single test takes ~3s against a 2000ms timeout.

## Why pre-existing
- Fails identically with my changes stashed (clean tree at HEAD 89fe42c6) — verified via `git stash` / re-run / `git stash pop`.
- It is a wall-clock timeout in the optimizer's inclusion-dependency soundness suite, a subsystem this ticket never touches. My diff is confined to canonical DDL column-COLLATE emission (`schema/ddl-generator.ts`) plus two test files.

## Work-around
None needed — left untouched. My collation tests were validated via `--grep` so this unrelated timeout (which `--bail` would otherwise stop the suite on) did not block confirmation.
