# Pre-existing lint error

## Command
```
cd packages/quereus && yarn lint
```

## Failing test / location
`test/runtime/fork-contract.spec.ts:48`

## Error excerpt
```
error TS1360: Type '{ ... }' does not satisfy the expected type 'Record<keyof RuntimeContext, ForkPolicy>'.
  Property 'signal' is missing in type '{ ... }' but required in type 'Record<keyof RuntimeContext, ForkPolicy>'.
```

## Why pre-existing
The file `test/runtime/fork-contract.spec.ts` is not in the diff for this ticket.
`git diff --name-only HEAD` lists only `packages/quereus/src/runtime/emit/table-valued-function.ts`
(plus other files that were already modified before this ticket started).
This type error is a missing `signal` property in a test fixture, unrelated to TVF row padding.

## Workaround
The lint step errors but `yarn test` (Mocha) passes cleanly (6399 passing, 9 pending).
The regression tests added by this ticket run successfully.
