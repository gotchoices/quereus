# Overnight tess tending notes

## 2026-06-17 ~23:05 (wake 1)

- **Inheritree 0.4.0**: dep swap done earlier and committed (`84998e15` dep + `1bd75acc` ticket annotation). Full `@quereus/quereus` suite 1070 passing.
- **Prior runner anomaly**: found TWO `node tess/scripts/run.mjs` processes ALIVE but INERT
  (PID 39888 started 16:34, PID 54636 started 21:32) — no commits or `.logs` growth for 32+ min
  while `plan/` (4) + `implement/` (1) sat queued, and the git tree was clean. `run.mjs` has no
  single-instance guard/lockfile, so a concurrent fresh runner would have risked commit races.
- **Action**: killed both `run.mjs` process trees (clean tree ⇒ zero work lost), then started ONE
  fresh `node tess/scripts/run.mjs` in the background.
- **FK-RESTRICT item**: the nested-cascade scope flagged at the `sync-fk-actions-restrict-failure-policy`
  sign-off was carried implement→review by the prior runner (`760a68b5 ticket(review):
  sync-fk-actions-restrict-skip-on-apply`). **MORNING CHECK**: confirm that review actually resolved
  the `external-row-change-ingestion.spec.ts` "FK RESTRICT on apply" failure rather than deferring it.
- **Backlog root**: empty at this wake (nothing to triage / feed).

## 2026-06-18 ~02:57 (wake 5 — runner exit + restart)

- Wake-1 runner (background task brjl0xv8i) finished ALL pipeline work (tickets 9/10/11 → complete, plus several spawned leak-fixes) and exited cleanly (exit 0); pipeline fully drained.
- Fed 3 spawned backlog tickets → plan/: `1-sync-drain-scoped-on-reappear`, `2-quoomb-web-lint-no-eslint-config` (DECISION recorded: retire the vestigial lint scripts via no-op echo — see ticket), `3-extract-sync-e2e-test-harness`. Commit b9956ca7.
- Restarted runner → PID 24328 (now planning ticket 1).
- **Orphan cleanup (FYI for runner management):** found PID 54676 — a child of the wake-1 runner, started 23:11 — still alive AFTER its parent exited, with a 7-process descendant tree. So `node tess/scripts/run.mjs` can leave orphaned child/agent processes on exit. It was dormant all night (pipeline progressed cleanly, no double-commits), but I killed its tree (git tree was clean ⇒ zero work lost) to guarantee exactly one active runner. Going forward, after a runner exits, check for orphaned run.mjs descendants before relying on a clean slate.
- Healthy overnight so far: FK-RESTRICT skip work, store-coordinator-stats-callback-leak, store-backing-host-connection-owner-leak, store-module-wide-coordinator, store-adopt-atomic-gate-drop all landed; one pre-existing-failure triage ran (392b235f).

## 2026-06-18 ~05:01 (wake 8 — pipeline fully drained, going idle)

- Runner completed the LAST queued ticket (`sync-drain-reappear-lens-redeploy-e2e` → complete). Pipeline AND backlog root are now BOTH EMPTY; the test-only ticket spawned no follow-ups.
- Per loop policy, did NOT restart into an empty pipeline. Killed the exited runner's orphan tree (PID 43040, 13 procs; tree clean ⇒ no loss) so no stale run.mjs lingers to confuse runner-detection. **No runner is currently running — intentional idle.**
- The hourly idle wake stays scheduled; it will start a fresh runner only when new backlog/pipeline work appears.
- Overnight throughput (branch view-updates-lens): inheritree 0.4.0 dep swap; sync-fk-actions-restrict-skip-on-apply; store atomic-commit chain (module-wide-coordinator, adopt-gate-drop) + 2 coordinator/backing-host leak fixes; sync-assertion-violation-emit-before-abort; sync-basis-lifecycle-sql-tvf; full scoped-drain feature (host-wiring, inbound-ddl, local-ddl, lens-redeploy + e2e); quoomb-web lint retired; sync e2e harness extracted. All committed; tree clean.
- FK-RESTRICT test note resolved: the `sync-fk-actions-restrict-skip-on-apply` review (760a68b5) completed, and review can't pass with failing tests — so `external-row-change-ingestion.spec.ts` is green. Optional morning spot-check only.

## 2026-06-18 ~05:21 (wake 9 — anomalous hung runner cleaned)

- Despite leaving the system idle at wake 8 (no runner), a `node tess/scripts/run.mjs` (PID 10096) appeared at 05:03:35 — ~30s after my wake-8 idle commit (ff8aa661); I did NOT launch it. Parent was a Claude Bash run_in_background wrapper (PID 32260). It spawned a `cmd.exe` child (49060) with a deep process subtree but produced ZERO log/commit output in ~18 min on an EMPTY pipeline+backlog. The healthy runner exits within seconds on empty work (observed 3× tonight), so this instance was HUNG (likely blocked on the cmd.exe shell-out).
- Killed its full tree (13 procs). No work lost: pipeline+backlog empty, git tree clean, no stale .git/index.lock. Clean idle restored — no run.mjs running.
- **MORNING — investigate runner lifecycle:** an unexplained run.mjs appeared after the previous runner exited, then hung. Tonight's recurring per-exit "orphans" are likely related (each exited runner left a child run.mjs tree). Suggest a single-instance lock for run.mjs and a non-interactive git invocation (GIT_TERMINAL_PROMPT=0; avoid cmd.exe prompts) so a successor/orphan cannot hang.
- Pipeline + backlog remain EMPTY; staying idle (did not restart). Hourly idle wake continues.

## 2026-06-18 ~06:27 (wake 10 — respawn source is EXTERNAL; root-cause narrowed)

- A 3rd anomalous `run.mjs` (PID 24684, started 06:14:50) appeared and hung (alive ~13 min, empty pipeline, no commits/logs). Killed its tree.
- NARROWED: these hung run.mjs are launched by Claude Bash run_in_background wrappers in a nested-wrapper chain (parent AND grandparent run the identical `eval node tess/scripts/run.mjs`). BUT `TaskStop` on all three runner tasks I launched (brjl0xv8i, b2cdlmmm9, bs78vobge) returns "No task found" — they are fully completed/gone. So the respawner is NOT my tracked background tasks.
- CONCLUSION: an EXTERNAL relaunch mechanism (most likely your own runner loop/watchdog — the "tess runner heading for a cycle stop" at the start may be self-relaunching) spawns run.mjs ~hourly, and each cycle HANGS on Windows (a cmd.exe/git shell-out that never returns) instead of exiting on the empty pipeline. I cannot stop this source from inside the session.
- IMPACT: harmless — pipeline+backlog empty, git tree clean, no work lost. Pure noise.
- MORNING ACTIONS: (1) find & stop the runner relaunch loop/watchdog; (2) add a single-instance lock to run.mjs; (3) make its git calls non-interactive (GIT_TERMINAL_PROMPT=0; avoid cmd.exe prompt hangs) so a cycle cannot hang. All overnight ticket work is DONE and committed; the pipeline is fully drained.

## 2026-06-18 ~08:35 (wake 12 — NEW: a backlog/known/ ticket was deleted by the respawn)

- Killed the hourly hung run.mjs (PID 26644, 13-proc tree) as usual.
- NEW (not the known respawn-noise): the working tree has ONE unstaged deletion — `tickets/backlog/known/set-op-membership-ergonomic-extensions.md` (` D`). I did NOT delete it (I never touch known/). It appeared this past hour (tree was clean at wake 11), coinciding with respawn PID 26644 (started 07:42), so the hung-runner mechanism is the likely culprit. Looks like a half-finished ticket move (source deleted, no corresponding new file, never committed) abandoned when the cycle hung.
- The file is SAFE — still committed in HEAD. Recover with: `git checkout -- tickets/backlog/known/set-op-membership-ergonomic-extensions.md`
- Per git-hygiene I did NOT revert it (don't undo uncommitted changes I didn't make) and did NOT commit it — left for your decision.
- ESCALATES the wake-10 finding: the respawned runners are NOT purely inert — at least one MUTATED the repo (deleted a ticket) before hanging. Low impact (known/ item, recoverable, pipeline unaffected), but reinforces stopping the external relaunch loop + adding a single-instance lock as the morning priority.

## 2026-06-18 ~09:41 (wake 13 — clean idle; no runner this hour)

- NO `run.mjs` running this wake (no healthy/hung/orphan instance — the hourly respawn did not fire in this window). Pipeline AND backlog root both EMPTY → stayed idle, started nothing.
- Wake-12 `known/` deletion is RESOLVED: committed as an intentional removal in `12157d81` ("Removed set op exists flag ticket"). No longer in the working tree.
- Only tree change now is `?? CHANGELOG.md` — this is the USER's interactive-session release-prep work (4.0.0 assessment), NOT a runner artifact (note the `??` add vs. the respawn's tickets/ deletions, and the user has since dated its heading). Left untouched per git-hygiene; not mine to commit. Future wakes: ignore it.
- Nothing new/wedged in the pipeline. Hourly idle wake continues.
